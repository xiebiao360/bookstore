┌─────────────────────────────────────────────────────────────────┐
│                  图书商城微服务 - 快速参考                       │
└─────────────────────────────────────────────────────────────────┘

🚀 一键启动
─────────────────────────────────────────────────────────────────
  make start              # 启动所有服务
  make stop               # 停止所有服务
  make restart            # 重启所有服务
  make logs               # 查看所有日志
  make help               # 查看所有命令

📊 访问地址
─────────────────────────────────────────────────────────────────
  基础设施:
    • MySQL:          localhost:3306 (bookstore/bookstore123)
    • phpMyAdmin:     http://localhost:8081
    • Redis:          localhost:6379 (密码: redis123)
    • RabbitMQ管理:   http://localhost:15672 (admin/admin123)
    • Jaeger UI:      http://localhost:16686

  微服务:
    • API Gateway:    http://localhost:8080
    • User Service:   grpc://localhost:50051
    • Catalog Svc:    grpc://localhost:50052
    • Inventory Svc:  grpc://localhost:50053
    • Payment Svc:    grpc://localhost:50054
    • Order Service:  grpc://localhost:50055

🧪 快速测试
─────────────────────────────────────────────────────────────────
  # 健康检查
  curl http://localhost:8080/health

  # 用户注册
  curl -X POST http://localhost:8080/api/v1/users/register \
    -H "Content-Type: application/json" \
    -d '{"email":"test@example.com","password":"pass123","nickname":"测试"}'

  # 用户登录
  curl -X POST http://localhost:8080/api/v1/users/login \
    -H "Content-Type: application/json" \
    -d '{"email":"test@example.com","password":"pass123"}'

📝 日志文件
─────────────────────────────────────────────────────────────────
  logs/api-gateway.log        logs/user-service.log
  logs/catalog-service.log    logs/inventory-service.log
  logs/payment-service.log    logs/order-service.log

🔧 开发命令
─────────────────────────────────────────────────────────────────
  make build              # 编译所有服务
  make test               # 运行所有测试
  make clean              # 清理编译产物
  make docker-up          # 仅启动基础设施
  make docker-down        # 停止基础设施

📚 文档
─────────────────────────────────────────────────────────────────
  README.md               # 项目概览
  ROADMAP.md              # 学习路径
  TEACHING.md             # 教学原则
  docs/quick-start-guide.md           # 详细启动指南
  docs/phase2-complete-summary.md     # Phase 2总结

🎯 核心技术栈
─────────────────────────────────────────────────────────────────
  • gRPC + Protocol Buffers     # 服务间通信
  • Saga模式                    # 分布式事务
  • Circuit Breaker             # 熔断器
  • RabbitMQ                    # 消息队列
  • OpenTelemetry + Jaeger      # 分布式追踪
  • Prometheus                  # 监控指标

🐛 故障排查
─────────────────────────────────────────────────────────────────
  # 查看进程状态
  ps aux | grep -E "service|gateway"

  # 检查端口占用
  lsof -i :8080
  lsof -i :50051

  # 查看Docker容器
  docker compose ps
  docker compose logs -f <service>

  # 重启特定服务
  kill $(cat logs/order-service.pid)
  ./services/order-service/bin/order-service &

💡 提示
─────────────────────────────────────────────────────────────────
  1. 首次启动需要等待约30-60秒
  2. 查看Jaeger UI了解服务调用链路
  3. RabbitMQ管理界面可查看消息队列状态
  4. 所有服务日志实时写入logs/目录
  5. 完整文档见 docs/quick-start-guide.md

└─────────────────────────────────────────────────────────────────┘
